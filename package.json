{
  "name": "oraculo-radiohead",
  "version": "0.1.0",
  "private": true,
  "description": "Lie detector MCP for AI agents — seal contradictions, block USDT on FALSE (Aleph 2026)",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @oraculo/market-core && npm run build -w @oraculo/mcp-server && npm run build -w @oraculo/mcp-lite && npm run build -w @oraculo/demo-web",
    "build:core": "npm run build -w @oraculo/market-core",
    "dev:mcp": "npm run build:core && npm run start -w @oraculo/mcp-server",
    "dev:web": "npm run build:core && npm run dev -w @oraculo/demo-web",
    "start:mcp": "npm run start -w @oraculo/mcp-server",
    "start:mcp:lite": "npm run start -w @oraculo/mcp-lite",
    "typecheck": "npm run typecheck -w @oraculo/market-core && npm run typecheck -w @oraculo/mcp-server && npm run typecheck -w @oraculo/mcp-lite",
    "contracts:compile": "hardhat compile",
    "contracts:deploy:local": "hardhat run contracts/scripts/deploy.js --network hardhat",
    "contracts:deploy:base": "hardhat run contracts/scripts/deploy.js --network baseSepolia",
    "contracts:deploy:sepolia": "hardhat run contracts/scripts/deploy.js --network sepolia"
  },
  "engines": {
    "node": ">=20"
  },
  "author": "Dax Kenji Tellez Duran",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "dotenv": "^17.4.2",
    "ethers": "^6.13.5",
    "hardhat": "^2.22.19"
  }
}
